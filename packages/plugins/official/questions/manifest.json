{
  "id": "questions",
  "version": "0.1.0",
  "description": "Collect structured answers via interactive questionnaires.",
  "surfaces": { "tool": true, "http": true, "cli": true },
  "server": {
    "provides": ["questions"],
    "capabilities": ["questions.ask"]
  },
  "operations": [
    {
      "id": "ask",
      "summary": "Ask the user a structured questionnaire using schema.fields or schema.sections.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Optional prompt shown above the questionnaire."
          },
          "schema": {
            "type": "object",
            "description": "Questionnaire schema. Provide either schema.fields or schema.sections. Valid field types: text, textarea, number, boolean, select, multiselect, checkbox, radio, date, time, datetime. For select, radio, and multiselect fields, options must be objects with { label, value }.",
            "properties": {
              "title": { "type": "string" },
              "description": { "type": "string" },
              "fields": {
                "type": "array",
                "items": { "$ref": "#/definitions/field" }
              },
              "sections": {
                "type": "array",
                "items": { "$ref": "#/definitions/section" }
              },
              "submitLabel": { "type": "string" },
              "cancelLabel": { "type": "string" },
              "initialValues": {
                "type": "object",
                "description": "Prefill values keyed by field id.",
                "additionalProperties": true
              }
            }
          },
          "validate": {
            "type": "boolean",
            "description": "When true (default), validate responses against the schema and reprompt on errors."
          }
        },
        "required": ["schema"],
        "definitions": {
          "field": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "type": {
                "type": "string",
                "enum": [
                  "text",
                  "textarea",
                  "number",
                  "boolean",
                  "select",
                  "multiselect",
                  "checkbox",
                  "radio",
                  "date",
                  "time",
                  "datetime"
                ]
              },
              "label": { "type": "string" },
              "description": { "type": "string" },
              "required": { "type": "boolean" },
              "placeholder": { "type": "string" },
              "defaultValue": { "description": "Default value when no initialValues override is set." },
              "validateOnClient": { "type": "boolean" },
              "options": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "label": { "type": "string" },
                    "value": { "type": "string" }
                  },
                  "required": ["label", "value"]
                }
              },
              "min": { "type": "number" },
              "max": { "type": "number" },
              "step": { "type": "number" },
              "minLength": { "type": "number" },
              "maxLength": { "type": "number" },
              "pattern": { "type": "string" }
            },
            "required": ["id", "type", "label"]
          },
          "section": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "title": { "type": "string" },
              "description": { "type": "string" },
              "fields": {
                "type": "array",
                "items": { "$ref": "#/definitions/field" }
              },
              "optional": { "type": "boolean" },
              "submitLabel": { "type": "string" }
            },
            "required": ["id", "fields"]
          }
        }
      },
      "capabilities": ["questions.ask"],
      "tool": {
        "description": "Ask the user a questionnaire and return their answers. Define inputs with either schema.fields or schema.sections[].fields. Valid field types are text, textarea, number, boolean, select, multiselect, checkbox, radio, date, time, and datetime. For select, radio, and multiselect fields, options must be objects with { label, value }, for example: { id: \"color\", type: \"select\", label: \"Favorite color\", options: [{ label: \"Red\", value: \"red\" }, { label: \"Blue\", value: \"blue\" }] }."
      }
    }
  ]
}
